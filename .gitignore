# ── Python ───────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.pyc
*.egg-info/
.ipynb_checkpoints/
.pytest_cache/

# ── Model weights / checkpoints (download separately — see README) ───
checkpoints/
sam-3d-body-dinov3/
*.ckpt
*.pt
*.pth
*.safetensors

# ── TensorRT / ONNX engines (build locally — see README) ─────────────
*.engine
*.onnx
*.trt
*.plan
backbone_trt/

# ── Outputs (videos, keypoints, renders) ─────────────────────────────
output/
output_*/
*.mp4
*.avi
*.mov
*.npy
*.npz

# ── Capture / recording data (calibration frames, recorded takes) ────
stereo_calibration/images/
recordings/

# ── Experiment artifacts ─────────────────────────────────────────────
mhr2smpl/experiments/
mhr2smpl/output_visualization/
wandb/
*.log

# ── OS / editor ──────────────────────────────────────────────────────
.DS_Store
*.swp
.vscode/
.idea/

# ── Claude Code local settings (per-machine, not shared) ─────────────
.claude/settings.local.json
